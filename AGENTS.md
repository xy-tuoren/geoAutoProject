# AGENTS.md

本文件适用于整个仓库。修改代码前先阅读本文件和 `README.md`；若子目录以后出现更具体的 `AGENTS.md`，以距离目标文件最近的规则为准。

## 项目目标

本项目是一个 Electron/Node.js Android 自动化工具，用于在小荷 AI 医生中批量提问、等待回答稳定、生成长截图并归档 UI 层级和元数据。

核心目标按优先级排序：

1. 不漏掉回答正文、引用资料或任何参考药品。
2. 不误输入、误发送、误开新会话或操作错误应用。
3. 截图接缝可验证；无法验证时宁可明确失败，也不能静默裁掉内容。
4. 在完整性不降低的前提下减少固定等待、重复截图和无效滚动。

## 运行环境与结构

- Node.js 22+，CommonJS 模块。
- Electron 主进程：`src/main/`
- 操作面板：`src/renderer/`
- 自动化核心：`src/automation/`
- CLI：`src/cli.js`
- Python uiautomator2 sidecar：`python/`
- Node 测试：`tests/node/`
- 运行时构建脚本：`scripts/`

关键文件：

- `src/automation/runner.js`：提问、等待、滚动、截图和归档状态机。
- `src/automation/hierarchy.js`：UI 层级解析和结构识别。
- `src/automation/images.js`：图片稳定性、重叠验证和长图拼接。
- `src/automation/scrcpy-observer.js`：低分辨率画面活动观察器。
- `src/automation/u2-client.js`：Python uiautomator2 sidecar 客户端。

## 不可破坏的自动化约束

### UI 与截图后端

- 点击、输入和层级读取必须使用 Python uiautomator2 sidecar。
- ADB 可用于设备连接、原始 PNG 截图和滑动命令，但不能作为点击或输入失败后的隐式降级方案。
- 正式截图只能使用 ADB 原始 PNG；不要使用 uiautomator2 或 scrcpy 画面作为输出图片。
- scrcpy 只用于判断画面活动/静止，不解码、不保存、不参与拼接。
- UI 层级必须属于 `com.aurora.xiaohe.aidoctor`；前台不是目标应用时立即停止 UI 操作。
- 有副作用的操作失败后不要盲目重放。只读层级读取可以有限重试。

### 当前已有回答模式

`--capture-current-answer` 必须满足：

- 不新建会话。
- 不聚焦输入框。
- 不输入或发送任何内容。
- 自动识别当前已有问题，只处理该问题已经生成的回答。
- 药品标签（如“医保甲类”）不能被误识别成用户问题。

### 回答截图状态机

截图必须严格单向执行：

1. 定位当前问题顶部。
2. 在回答第一帧前展开可见引用资料，并将其纳入回答长图。
3. 从上向下采集回答正文并验证相邻截图接缝。
4. 遇到固定在回答尾部的参考药品入口后，进入药品采集。
5. 参考药品完整采集后，本题立即结束。

禁止在药品采集后重新定位、恢复或比对正文锚点。参考药品是回答的终止序列，不需要药品后的正文扫描。

### 参考药品列表

- 打开抽屉后，先在标题/拖拽区域向上展开 BottomSheet。
- 必须确认列表已经进入全屏视口，之后才允许截取第一张药品图片。
- 不要在药品卡片内容区拖动来展开抽屉，否则可能从中间药品开始。
- 不要通过向下反滑“回到顶部”；BottomSheet 会拦截手势并可能收起或关闭。
- 药品必须从首项持续采集到真实末项，所有图片必须确认加载完成。
- 不得设置药品页数上限。结束条件只能是滚动后稳定画面连续无变化。
- 首屏短列表需使用多个触点确认无法滚动；发生过滚动后，末屏至少连续两次无变化才确认到底。
- 药品拼接接缝必须经过像素验证；只有前序位移稳定时才能使用校准后的保守重叠。
- 药品长图不得插入灰色分隔线。超过单图高度时按视口边界分片，所有分片共同组成完整列表。
- 采集完成后可以关闭抽屉用于清理界面，但不能再执行正文锚点恢复或相似度校验。

## 稳定性与性能原则

- 优先使用 scrcpy 活动标记进行事件驱动的静止判断，不要恢复为固定约 4 秒/屏等待。
- scrcpy 无法确认时使用“ADB PNG → UI 层级 → ADB PNG”夹心校验。
- 复用已经稳定的无损 PNG 做图片加载和拼接判断，避免为同一状态重复截图。
- 每个普通回答接缝最多局部重采一次；仍无法验证时保留完整下一视口并明确记录安全降级。
- 参考药品接缝不得因一次失败退化为整段全视口堆叠。
- 优化耗时时必须同时保留：首项纳入、图片就绪、真实到底、接缝连续这四个完成条件。

## 开发工作流

修改前：

1. 执行 `git status --short`，保留用户已有改动。
2. 使用 `rg` 查找相关代码和测试。
3. 对缺陷先建立可重复的单元测试或最小诊断信号。

修改文件时使用补丁式编辑，避免覆盖无关代码。不要提交 `captures/`、健康问题、截图或其他可能包含个人信息的文件。

常用检查命令：

```bash
npm run check
npm test
git diff --check
```

涉及真机状态机、滚动、截图或性能的改动，单元测试通过后还需要一次真机验收。除非用户明确要求，不要为了测试发送新问题；优先复用当前已有回答：

```bash
npm run run:android -- \
  --serial <设备序列号> \
  --output-dir ./captures/current-answer \
  --capture-current-answer
```

真机验收至少检查：

- 日志确认没有输入、发送或新建会话。
- 回答正文从问题开始，引用资料已经展开。
- 参考药品第一分片包含首项，最后分片包含末项。
- 没有错误的半屏首分片或灰色分隔线。
- JSON 中 `reference_products_capture_complete=true`。
- JSON 中图片就绪、确认到底、拼接连续均为 `true`。
- JSON 中 `reference_products_restore_required=false`、`reference_products_terminal_sequence=true`、`reference_products_post_scan_swipes=0`。

## Git 与交付

- 不要丢弃或重置用户改动。
- 提交前必须通过 `npm run check`、`npm test` 和 `git diff --check`。
- 提交信息应说明实际行为变化和根因，避免只有“fix”或“update”。
- 更新自动化行为时同步更新 `README.md`、元数据字段和回归测试。
- 最终回复应列出改动、验证结果、产物绝对路径，以及实际产生的 Git 提交（如果有）。

## 隐私与安全

- `captures/` 可能包含健康信息和设备界面数据，必须保持忽略状态。
- 不要把真机截图、回答正文、UI XML 或设备个人信息上传到公共仓库。
- 不执行破坏性 Git 命令，不清理不属于本次任务的文件。
